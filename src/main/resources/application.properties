spring.application.name=Practica_para_la_PC1

# Servidor
server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/bibliobarrio
spring.datasource.username=postgres
spring.datasource.password=214354
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
jwt.secret=bibliobarrio_secret_key_muy_larga_para_seguridad_2025
jwt.expiration=3600000